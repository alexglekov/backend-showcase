export interface SchemaRegistryConfig {
  schemaRegistry: {
    apolloStudio: {
      graphId: string;
      variantName: string;
      subgraphName: string;
      apiKey: string;
      revision: string;
    };
  };
}
