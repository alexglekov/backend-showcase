{
  "name": "setups-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/setups-worker/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/setups-worker",
        "main": "apps/setups-worker/src/main.ts",
        "tsConfig": "apps/setups-worker/tsconfig.app.json",
        "assets": [
          {
            "glob": "**/**/*.proto",
            "input": "libs/contracts/src",
            "output": "grpc"
          }
        ],
        "isolatedConfig": true,
        "webpackConfig": "apps/setups-worker/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "setups-worker:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "setups-worker:build:development"
        },
        "production": {
          "buildTarget": "setups-worker:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/setups-worker/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/setups-worker/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
